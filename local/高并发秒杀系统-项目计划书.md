# 高并发秒杀系统 — 项目计划书

> 面向简历升级的全栈项目，覆盖从用户点击到订单完成的全链路秒杀场景。

---

## 一、项目定位

### 1.1 一句话描述

从零构建一个支撑万级 QPS 的秒杀系统，重点解决**库存热点**、**流量洪峰**、**缓存一致性**三个核心问题，包含后端服务 + 前端页面 + 管理后台。

### 1.2 与现有简历项目的区别

| 维度   | 现有项目            | 升级后                     |
| ---- | --------------- | ----------------------- |
| 库存方案 | 单 Key Redis 扣减  | 库存分桶 + Lua 原子扣减         |
| 流量防护 | 通用限流 + Sentinel | 四层漏斗过滤（验证码→Nginx→后端→MQ） |
| 下单方式 | MQ 异步           | 本地队列 + 优雅退出 + 兜底补偿      |
| 热点治理 | 无               | 本地缓存 + Key 打散           |
| 前端   | 无               | 完整秒杀页面 + 轮询 + 管理后台      |
| 可演示性 | 仅后端             | 全链路可浏览器演示               |

### 1.3 面试展开点

- 库存分桶：为什么不用单 Key？Redis 单分片写瓶颈？分桶后库存不均匀怎么处理？
- 分层过滤：每层过滤了什么？为什么限流要前置？
- 优雅退出：本地队列数据丢失怎么办？兜底补偿和优雅退出的关系？
- 缓存一致性：缓存和 DB 的库存怎么保持一致？

---

## 二、系统架构

### 2.1 整体架构图

```
                        ┌──────────────────────────┐
                        │        用户浏览器          │
                        │  验证码 + 倒计时 + 轮询结果  │
                        └────────────┬─────────────┘
                                     │
                        ┌────────────▼─────────────┐
                        │     Nginx（反向代理）       │
                        │  limit_req 限流            │
                        │  Lua 前置校验（库存=0直接返） │
                        │  静态资源服务（前端页面）     │
                        └────────────┬─────────────┘
                                     │
                ┌────────────────────┼────────────────────┐
                │                    │                    │
    ┌───────────▼──────┐ ┌──────────▼───────┐ ┌─────────▼──────────┐
    │   秒杀服务 (BFF)   │ │   商品服务        │ │   管理后台 API       │
    │  Sentinel 限流     │ │  商品详情查询      │ │  活动/库存管理       │
    │  前置校验          │ │  本地缓存(Caffeine)│ │                    │
    │  本地队列          │ │                   │ │                    │
    │  Lua 脚本扣库存    │ │                   │ │                    │
    └───────┬──────────┘ └────────┬──────────┘ └─────────┬──────────┘
            │                     │                      │
            ▼                     ▼                      ▼
    ┌───────────────┐    ┌───────────────┐      ┌───────────────┐
    │    RocketMQ    │    │  Redis Cluster │      │     MySQL      │
    │  异步下单消息   │    │  分桶库存       │      │  商品/活动/订单  │
    │  延迟消息(过期) │    │  Lua 原子扣减   │      │               │
    └───────┬───────┘    └───────────────┘      └───────────────┘
            │
    ┌───────▼───────┐
    │  订单消费者     │
    │  幂等校验       │
    │  订单落库       │
    └───────────────┘
```

### 2.2 请求链路（用户视角）

```
用户点击"立即抢购"
    ↓
弹出验证码（图片验证码 / 滑块）
    ↓ 用户输入正确
携带验证码 Token 发起秒杀请求
    ↓
Nginx：IP 限流 + Lua 查 Redis 库存是否为 0
    ↓ 通过
Spring Boot：Sentinel 限流 → 前置校验（活动状态/是否已购/本地缓存库存）
    ↓ 通过
Redis Lua 脚本：原子扣减分桶库存
    ↓ 扣减成功
构建订单数据 → 投递本地队列 → 立即返回"排队中，请等待"
    ↓
客户端每 2 秒轮询订单状态
    ↓ 同时
消费者从本地队列取数据 → 限速投递 RocketMQ → 消费者幂等落库
    ↓
轮询拿到结果 → 显示"抢购成功，请支付" 或 "已售罄"
```

---

## 三、后端模块详细设计

### 3.1 库存分桶模块

**核心思路**：将单个 Redis Key 拆为 N 个子 Key，用户请求通过取模路由到不同子 Key，分散写压力。

**数据结构设计**：

```
# 活动库存总信息（Hash）
seckill:stock:{activityId}  →  totalCount: 1000,  bucketCount: 5

# 分桶库存（String，每个桶一个 Key）
seckill:stock:{activityId}:0  →  200
seckill:stock:{activityId}:1  →  200
seckill:stock:{activityId}:2  →  200
seckill:stock:{activityId}:3  →  200
seckill:stock:{activityId}:4  →  200

# 用户去重（Set，防同一用户重复扣减）
seckill:bought:{activityId}  →  {userId1, userId2, ...}
```

**Lua 扣减脚本**：

```lua
-- KEYS[1] = 分桶库存 Key, KEYS[2] = 用户去重 Set Key
-- ARGV[1] = userId
-- 返回: 1=成功, 0=库存不足, -1=已购买过

-- 幂等校验：是否已购买
if redis.call('SISMEMBER', KEYS[2], ARGV[1]) == 1 then
    return -1
end

-- 库存扣减
local stock = tonumber(redis.call('GET', KEYS[1]))
if stock == nil or stock <= 0 then
    return 0
end

redis.call('DECR', KEYS[1])
redis.call('SADD', KEYS[2], ARGV[1])
return 1
```

**路由逻辑**：

```java
int bucket = (int) (userId % bucketCount);
String stockKey = "seckill:stock:" + activityId + ":" + bucket;
String boughtKey = "seckill:bought:" + activityId;
```

**分桶库存不均匀的处理**：
- 秒杀场景下大部分桶会被瞬间抢空，概率极低出现不均匀
- 极端情况：后台管理界面支持手动收缩库存（将未售罄的桶合并）

### 3.2 分层流量过滤

#### 第 1 层：前端验证码

- 用户点击抢购按钮 → 弹出图片验证码
- 后端生成 6 位随机字符串，存入 Redis（TTL 5s），绘入图片返回前端
- 用户输入后提交，后端使用 `GETDEL` 校验（无论对错立即失效，防暴力破解）
- **消峰效果**：用户手速差异将 0.5 秒内的并发分散到几秒

#### 第 2 层：Nginx 限流 + Lua 前置校验

```nginx
# IP 限流：每秒每 IP 最多 5 个请求
limit_req_zone $binary_remote_addr zone=seckill:10m rate=5r/s;

location /api/seckill/order {
    limit_req zone=seckill burst=10 nodelay;

    # Lua 前置校验：直接查 Redis 判断库存是否为 0
    # 为 0 则直接返回 {"code": 4100, "msg": "已售罄"}
    # 不为 0 则放行到后端
    access_by_lua_block {
        -- 从请求参数获取 activityId
        local activityId = ngx.var.arg_activityId
        local key = "seckill:stock:" .. activityId
        -- 查询所有分桶总库存（用 GET 累加或维护一个总库存 Key）
        local total = redis.call("GET", "seckill:total:" .. activityId)
        if tonumber(total) <= 0 then
            ngx.say('{"code":4100,"msg":"已售罄"}')
            return ngx.exit(200)
        end
    }
}
```

#### 第 3 层：后端前置校验

在进入核心扣减逻辑前，依次校验：

1. **活动状态**：是否在秒杀时间窗口内（从本地缓存读取，避免每次查 DB）
2. **用户是否已购**：先查本地缓存的布隆过滤器，再查 Redis Set
3. **本地缓存库存**：Caffeine 缓存库存余量（TTL 3s），快速判断是否充足

任一不满足 → 直接拒绝，不消耗 Redis/DB 资源。

#### 第 4 层：Sentinel 限流熔断

```yaml
# Sentinel 规则
spring:
  cloud:
    sentinel:
      datasource:
        seckill-rule:
          nacos:
            rule-type: flow
            data-id: seckill-flow-rules
            group-id: SECKILL
```

规则配置：
- 秒杀下单接口：QPS 上限 5000（单机）
- 异常比例 > 20% 时自动熔断，5s 后半开探测
- 降级返回：`{"code": 429, "msg": "系统繁忙，请稍后再试"}`

### 3.3 异步下单模块

#### 本地队列设计

```java
@Component
public class SeckillLocalQueue {
    // 有界阻塞队列，容量 10000
    private final BlockingQueue<SeckillOrderMsg> queue = new LinkedBlockingQueue<>(10000);

    // 生产者：扣减库存成功后投递
    public boolean offer(SeckillOrderMsg msg) {
        return queue.offer(msg);  // 队列满则返回 false，直接拒绝
    }

    // 消费者：定时批量消费
    @Scheduled(fixedDelay = 100)  // 每 100ms 执行一次
    public void consume() {
        List<SeckillOrderMsg> batch = new ArrayList<>();
        queue.drainTo(batch, 100);  // 一次取最多 100 条
        if (!batch.isEmpty()) {
            // 合并相同活动的库存扣减（减少 Redis 写次数）
            // 发送到 RocketMQ
            rocketMQTemplate.syncSend("seckill-order-topic", batch);
        }
    }
}
```

#### 优雅退出

```java
@Component
public class GracefulShutdown {
    @Autowired
    private SeckillLocalQueue localQueue;
    @Autowired
    private RocketMQTemplate rocketMQTemplate;

    @PreDestroy
    public void onShutdown() {
        // 1. 停止消费队列
        localQueue.stopConsuming();

        // 2. 将队列中剩余数据转移到 RocketMQ
        List<SeckillOrderMsg> remaining = new ArrayList<>();
        localQueue.drainAll(remaining);
        if (!remaining.isEmpty()) {
            rocketMQTemplate.syncSend("seckill-order-topic", remaining);
        }
        // 由其他存活实例消费这些消息
    }
}
```

#### 兜底补偿

```java
@Component
public class OrderCompensationTask {
    // 每 30 秒扫描一次卡在"排队中"状态超过 60 秒的记录
    @Scheduled(fixedDelay = 30000)
    public void compensate() {
        List<SeckillOrder> stuckOrders = orderMapper.selectByStatusAndCreateTime(
            OrderStatus.PENDING, LocalDateTime.now().minusSeconds(60)
        );
        for (SeckillOrder order : stuckOrders) {
            // 重新投递到 MQ
            rocketMQTemplate.syncSend("seckill-order-topic", order.toMsg());
        }
    }
}
```

### 3.4 热 Key 治理

#### 本地缓存

```java
@Component
public class StockLocalCache {
    private final Cache<String, Long> cache = Caffeine.newBuilder()
        .expireAfterWrite(3, TimeUnit.SECONDS)   // 3 秒过期
        .maximumSize(1000)
        .build();

    public Long getStock(String activityId) {
        return cache.get(activityId, key -> {
            // 从 Redis 查询总库存
            return redisTemplate.opsForValue().get("seckill:total:" + key);
        });
    }
}
```

#### Key 打散（适用于读多写少的热 Key）

```
原始 Key:   seckill:detail:10001
打散后:     seckill:detail:10001:00 ~ seckill:detail:10001:99

读请求:  随机选一个后缀读取（本地缓存兜底）
写请求:  更新所有后缀（或通过 MQ 异步广播）
```

### 3.5 缓存一致性

```
库存设置流程（运营操作）：
  后台设置库存 → 写入 MySQL → 计算分桶 → 批量写入 Redis → 返回成功

库存扣减流程（用户抢购）：
  Redis Lua 扣减成功 → 构建订单消息 → MQ 异步落库 MySQL
  Redis 扣减失败 → 直接返回失败（不操作 DB）

一致性保障：
  - Redis 库存为主，MySQL 库存为辅
  - 定时对账任务：比对 Redis 总扣减数 vs MySQL 订单数
  - 差异时以 Redis 为准修正 MySQL（因为 Redis 是实际扣减方）
```

---

## 四、前端模块详细设计

### 4.1 技术选型

**方案：纯 HTML + CSS + JavaScript（原生）**

理由：
- 前端不是面试重点，轻量即可
- 直接部署在 Nginx 静态资源目录下，与 Nginx 限流方案自然结合
- 无需构建工具，面试时可快速演示

### 4.2 页面设计

#### 页面 1：秒杀商品列表页

```
┌─────────────────────────────────────────────────┐
│  🔥 限时秒杀                                     │
│                                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │  商品图片  │  │  商品图片  │  │  商品图片  │      │
│  │          │  │          │  │          │      │
│  │ iPhone15 │  │ AirPods  │  │  iPad    │      │
│  │ 原价5999 │  │ 原价1299 │  │ 原价2999 │      │
│  │ 秒杀价99 │  │ 秒杀价99 │  │ 秒杀价99 │      │
│  │          │  │          │  │          │      │
│  │ 库存: 200│  │ 库存: 500│  │ 库存: 100│      │
│  │ ██████░░ │  │ ████████ │  │ ███░░░░░ │      │
│  │          │  │          │  │          │      │
│  │ 距开始:  │  │ 立即抢购  │  │ 已售罄   │      │
│  │ 02:15:30 │  │   [GO]   │  │          │      │
│  └──────────┘  └──────────┘  └──────────┘      │
└─────────────────────────────────────────────────┘
```

核心组件：
- **倒计时组件**：活动未开始时显示"距开始 HH:MM:SS"，每秒刷新，到时间自动切换按钮状态
- **库存进度条**：`已售 / 总数`，用 CSS 宽度百分比展示
- **按钮状态机**：未开始（灰色禁用）→ 抢购中（高亮可点）→ 已售罄（灰色）

#### 页面 2：抢购流程弹窗

```
点击"立即抢购" 后的流程：

Step 1: 弹出验证码弹窗
┌─────────────────────────────┐
│  请输入验证码                 │
│  ┌─────────────────────┐    │
│  │  [图片: X7Kp2]  刷新 │    │
│  └─────────────────────┘    │
│  ┌─────────────────────┐    │
│  │  请输入验证码         │    │
│  └─────────────────────┘    │
│         [确认抢购]          │
└─────────────────────────────┘

Step 2: 验证通过 → 显示排队等待
┌─────────────────────────────┐
│  ⏳ 排队中，请稍候...        │
│                              │
│  ████████░░░░  预计 3 秒     │
│                              │
│  (每 2 秒自动轮询结果)       │
└─────────────────────────────┘

Step 3: 轮询拿到结果
┌─────────────────────────────┐
│  ✅ 抢购成功！               │
│                              │
│  订单号: SECKILL20260512xxx  │
│  商品: iPhone 15             │
│  秒杀价: ¥99                 │
│  请在 15 分钟内完成支付       │
│                              │
│        [去支付]  [取消]      │
└─────────────────────────────┘
```

#### 页面 3：简易管理后台

```
┌──────────────────────────────────────────────────┐
│  秒杀管理后台                                      │
├──────────────────────────────────────────────────┤
│                                                   │
│  活动管理                                          │
│  ┌────────────────────────────────────────────┐  │
│  │ 活动名称 │ 商品 │ 秒杀价 │ 库存 │ 分桶数│ 状态 │  │
│  │─────────┼──────┼───────┼──────┼──────┼──────│  │
│  │ 双11特惠 │ iPhone│ ¥99  │ 1000 │  5   │ 进行中│  │
│  │ 618大促  │ AirPods│ ¥49 │ 500  │  5   │ 未开始│  │
│  └────────────────────────────────────────────┘  │
│                                                   │
│  [新建活动]  [调整库存]  [手动收缩分桶]             │
│                                                   │
│  ─────────────────────────────────────────────── │
│                                                   │
│  实时监控                                          │
│  ┌──────────────────────────────────────┐        │
│  │  当前 QPS: 3,245    订单数: 823       │        │
│  │  库存余量: 177      缓存命中率: 96.2%  │        │
│  │                                      │        │
│  │  [QPS 曲线图 - 用 Chart.js 绘制]      │        │
│  │  [库存消耗趋势图]                      │        │
│  └──────────────────────────────────────┘        │
└──────────────────────────────────────────────────┘
```

### 4.3 前端核心代码结构

```
frontend/
├── index.html              # 秒杀商品列表页
├── detail.html             # 商品详情页（可选，或用弹窗替代）
├── admin.html              # 管理后台
├── css/
│   └── style.css           # 全局样式
├── js/
│   ├── api.js              # 封装 fetch 请求
│   ├── countdown.js        # 倒计时组件
│   ├── seckill.js          # 秒杀核心逻辑（验证码→抢购→轮询）
│   ├── carousel.js         # 库存进度条
│   └── admin.js            # 管理后台逻辑
└── assets/
    └── ...                 # 图片等静态资源
```

### 4.4 前端关键交互逻辑

**倒计时**：

```javascript
function startCountdown(targetTime, element) {
    const timer = setInterval(() => {
        const diff = targetTime - Date.now();
        if (diff <= 0) {
            clearInterval(timer);
            element.textContent = '立即抢购';
            element.disabled = false;
            element.classList.add('active');
            return;
        }
        const h = Math.floor(diff / 3600000);
        const m = Math.floor((diff % 3600000) / 60000);
        const s = Math.floor((diff % 60000) / 1000);
        element.textContent = `距开始 ${pad(h)}:${pad(m)}:${pad(s)}`;
    }, 1000);
}
```

**秒杀请求 + 轮询**：

```javascript
async function doSeckill(activityId) {
    // Step 1: 获取验证码
    const captcha = await getCaptcha();
    showCaptchaDialog(captcha.image);

    // Step 2: 用户输入验证码后提交
    const verifyResult = await verifyCaptcha(captcha.token, userInput);
    if (!verifyResult.success) {
        showError('验证码错误');
        return;
    }

    // Step 3: 发起秒杀请求
    showWaitingDialog('排队中，请稍候...');
    const seckillResult = await fetch('/api/seckill/order', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ activityId, captchaToken: captcha.token })
    }).then(r => r.json());

    if (seckillResult.code === 4100) {
        showResult('已售罄', 'fail');
        return;
    }

    // Step 4: 轮询订单结果
    const orderId = seckillResult.data.orderId;
    pollOrderResult(orderId);
}

function pollOrderResult(orderId) {
    const timer = setInterval(async () => {
        const res = await fetch(`/api/order/status?orderId=${orderId}`).then(r => r.json());
        if (res.data.status === 'SUCCESS') {
            clearInterval(timer);
            showResult('抢购成功！', 'success', res.data);
        } else if (res.data.status === 'FAILED') {
            clearInterval(timer);
            showResult(res.data.msg, 'fail');
        }
        // status === 'PENDING' 继续轮询
    }, 2000);

    // 超时保护：30 秒后停止轮询
    setTimeout(() => {
        clearInterval(timer);
        showResult('查询超时，请在订单中心查看', 'timeout');
    }, 30000);
}
```

---

## 五、数据库设计

### 5.1 核心表结构

```sql
-- 秒杀活动表
CREATE TABLE seckill_activity (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    activity_name   VARCHAR(100) NOT NULL COMMENT '活动名称',
    goods_id        BIGINT NOT NULL COMMENT '商品ID',
    goods_name      VARCHAR(200) NOT NULL COMMENT '商品名称',
    original_price  DECIMAL(10,2) NOT NULL COMMENT '原价',
    seckill_price   DECIMAL(10,2) NOT NULL COMMENT '秒杀价',
    total_stock     INT NOT NULL COMMENT '总库存',
    bucket_count    INT NOT NULL DEFAULT 5 COMMENT '分桶数量',
    start_time      DATETIME NOT NULL COMMENT '开始时间',
    end_time        DATETIME NOT NULL COMMENT '结束时间',
    status          TINYINT NOT NULL DEFAULT 0 COMMENT '0-未开始 1-进行中 2-已结束',
    create_time     DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time     DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_status_time (status, start_time, end_time)
) COMMENT '秒杀活动表';

-- 秒杀订单表
CREATE TABLE seckill_order (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_no        VARCHAR(64) NOT NULL UNIQUE COMMENT '订单号',
    activity_id     BIGINT NOT NULL COMMENT '活动ID',
    user_id         BIGINT NOT NULL COMMENT '用户ID',
    seckill_price   DECIMAL(10,2) NOT NULL COMMENT '秒杀价',
    status          TINYINT NOT NULL DEFAULT 0 COMMENT '0-待支付 1-已支付 2-已取消 3-已超时',
    create_time     DATETIME DEFAULT CURRENT_TIMESTAMP,
    pay_time        DATETIME COMMENT '支付时间',
    UNIQUE INDEX uk_activity_user (activity_id, user_id),
    INDEX idx_user_id (user_id),
    INDEX idx_status (status, create_time)
) COMMENT '秒杀订单表';

-- 库存流水表（用于对账）
CREATE TABLE stock_deduct_log (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    activity_id     BIGINT NOT NULL,
    user_id         BIGINT NOT NULL,
    bucket_index    INT NOT NULL COMMENT '分桶索引',
    deduct_time     DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_activity (activity_id, deduct_time)
) COMMENT '库存扣减流水';
```

### 5.2 用户表（简化）

```sql
CREATE TABLE user (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    username        VARCHAR(50) NOT NULL UNIQUE,
    password        VARCHAR(128) NOT NULL,
    nickname        VARCHAR(50),
    create_time     DATETIME DEFAULT CURRENT_TIMESTAMP
) COMMENT '用户表';
```

---

## 六、技术栈

| 层级   | 技术              | 版本    | 用途                 |
| ---- | --------------- | ----- | ------------------ |
| 语言   | Java            | 17    | 后端开发               |
| 框架   | Spring Boot     | 3.x   | Web 框架             |
| ORM  | MyBatis-Plus    | 3.5.x | 数据库访问              |
| 数据库  | MySQL           | 8.0   | 持久化存储              |
| 缓存   | Redis           | 7.x   | 库存缓存 + 分布式锁        |
| 消息队列 | RocketMQ        | 5.x   | 异步下单               |
| 限流熔断 | Sentinel        | 1.8.x | 流量控制 + 熔断降级        |
| 分布式锁 | Redisson        | 3.x   | 幂等校验               |
| 本地缓存 | Caffeine        | 3.x   | 热点数据本地缓存           |
| 前端   | HTML + CSS + JS | -     | 秒杀页面 + 管理后台        |
| 前端图表 | Chart.js        | 4.x   | 管理后台监控图表           |
| 反向代理 | Nginx           | 1.24+ | 限流 + 静态资源 + Lua 校验 |
| 部署   | Docker Compose  | -     | 一键部署全部组件           |

---

## 七、项目结构

```
seckill-system/
├── seckill-backend/                 # 后端 Spring Boot 项目
│   ├── src/main/java/com/seckill/
│   │   ├── SeckillApplication.java
│   │   ├── config/
│   │   │   ├── RedisConfig.java          # Redis 配置
│   │   │   ├── RocketMQConfig.java       # MQ 配置
│   │   │   ├── SentinelConfig.java       # 限流规则配置
│   │   │   └── CaffeineConfig.java       # 本地缓存配置
│   │   ├── controller/
│   │   │   ├── SeckillController.java    # 秒杀接口
│   │   │   ├── GoodsController.java      # 商品查询
│   │   │   ├── OrderController.java      # 订单查询
│   │   │   └── AdminController.java      # 管理后台 API
│   │   ├── service/
│   │   │   ├── SeckillService.java       # 秒杀核心逻辑
│   │   │   ├── StockService.java         # 库存管理（分桶、扣减、回补）
│   │   │   ├── OrderService.java         # 订单管理
│   │   │   └── CaptchaService.java       # 验证码服务
│   │   ├── mq/
│   │   │   ├── SeckillOrderProducer.java # 订单消息生产者
│   │   │   └── SeckillOrderConsumer.java # 订单消息消费者
│   │   ├── queue/
│   │   │   └── SeckillLocalQueue.java    # 本地队列 + 优雅退出
│   │   ├── cache/
│   │   │   └── StockLocalCache.java      # 库存本地缓存
│   │   ├── task/
│   │   │   └── OrderCompensationTask.java # 兜底补偿定时任务
│   │   ├── entity/
│   │   │   ├── SeckillActivity.java
│   │   │   ├── SeckillOrder.java
│   │   │   └── StockDeductLog.java
│   │   ├── mapper/
│   │   │   ├── SeckillActivityMapper.java
│   │   │   ├── SeckillOrderMapper.java
│   │   │   └── StockDeductLogMapper.java
│   │   ├── dto/
│   │   │   ├── SeckillRequest.java
│   │   │   ├── SeckillResponse.java
│   │   │   └── OrderStatusVO.java
│   │   └── lua/
│   │       ├── stock_deduct.lua           # 库存扣减 Lua 脚本
│   │       └── stock_check.lua            # 库存查询 Lua 脚本
│   ├── src/main/resources/
│   │   ├── application.yml
│   │   ├── application-dev.yml
│   │   └── mapper/                        # MyBatis XML
│   └── pom.xml
│
├── seckill-frontend/                # 前端静态页面
│   ├── index.html                       # 秒杀商品列表页
│   ├── admin.html                       # 管理后台
│   ├── css/
│   │   └── style.css
│   ├── js/
│   │   ├── api.js                       # 请求封装
│   │   ├── countdown.js                 # 倒计时组件
│   │   ├── seckill.js                   # 秒杀核心逻辑
│   │   └── admin.js                     # 管理后台逻辑
│   └── assets/
│
├── nginx/
│   └── nginx.conf                       # Nginx 配置（限流 + Lua）
│
├── docker-compose.yml                   # 一键部署
│   # MySQL + Redis + RocketMQ + Nginx + 后端服务
│
├── sql/
│   └── init.sql                         # 建表 + 初始数据
│
├── docs/
│   ├── architecture.md                  # 架构设计文档
│   ├── pressure-test.md                 # 压测报告
│   └── api.md                           # 接口文档
│
└── README.md
```

---

## 八、API 接口设计

### 8.1 秒杀相关

| 方法   | 路径                               | 说明                 |
| ---- | -------------------------------- | ------------------ |
| GET  | `/api/activity/list`             | 获取秒杀活动列表           |
| GET  | `/api/activity/{id}`             | 获取活动详情（含库存）        |
| POST | `/api/seckill/order`             | 发起秒杀下单（需验证码 Token） |
| GET  | `/api/order/status?orderId={id}` | 轮询订单状态             |

### 8.2 验证码相关

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/captcha/generate` | 生成验证码（返回图片 Base64 + Token） |
| POST | `/api/captcha/verify` | 校验验证码（配合秒杀请求） |

### 8.3 管理后台

| 方法   | 路径                                | 说明       |
| ---- | --------------------------------- | -------- |
| POST | `/api/admin/activity`             | 创建秒杀活动   |
| PUT  | `/api/admin/activity/{id}/stock`  | 调整库存     |
| POST | `/api/admin/activity/{id}/shrink` | 手动收缩分桶   |
| GET  | `/api/admin/monitor/qps`          | 获取实时 QPS |
| GET  | `/api/admin/monitor/stock`        | 获取库存消耗数据 |

---

## 九、部署方案

### 9.1 Docker Compose

```yaml
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    ports: ["3306:3306"]
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: seckill
    volumes:
      - ./sql/init.sql:/docker-entrypoint-initdb.d/init.sql

  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]

  rocketmq-namesrv:
    image: apache/rocketmq:5.1.4
    command: sh mqnamesrv
    ports: ["9876:9876"]

  rocketmq-broker:
    image: apache/rocketmq:5.1.4
    command: sh mqbroker -n rocketmq-namesrv:9876
    depends_on: [rocketmq-namesrv]

  seckill-backend:
    build: ./seckill-backend
    ports: ["8080:8080"]
    depends_on: [mysql, redis, rocketmq-broker]
    environment:
      SPRING_PROFILES_ACTIVE: dev

  nginx:
    image: nginx:1.24-alpine
    ports: ["80:80"]
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./seckill-frontend:/usr/share/nginx/html
    depends_on: [seckill-backend]
```

### 9.2 本地开发

```bash
# 启动基础设施
docker-compose up -d mysql redis rocketmq-namesrv rocketmq-broker

# 启动后端
cd seckill-backend && mvn spring-boot:run

# 启动前端（直接用 Nginx 或 VS Code Live Server）
# 访问 http://localhost
```

---

## 十、实施计划

### 10.1 里程碑

| 阶段           | 内容                                  | 预估工时 | 产出          |
| ------------ | ----------------------------------- | ---- | ----------- |
| **M1: 核心链路** | 库存分桶 + Lua 扣减 + 异步下单 + 订单落库         | 3 天  | 后端秒杀核心可运行   |
| **M2: 流量防护** | 分层限流（Nginx + Sentinel）+ 验证码 + 前置校验  | 2 天  | 四层过滤完成      |
| **M3: 前端页面** | 秒杀列表页 + 抢购弹窗 + 轮询结果                 | 2 天  | 浏览器可演示完整流程  |
| **M4: 运维能力** | 管理后台 + 优雅退出 + 兜底补偿 + Docker Compose | 2 天  | 一键部署 + 后台管理 |
| **M5: 压测验证** | JMeter 压测 + 数据采集 + 文档撰写             | 2 天  | 压测报告 + 简历数据 |

**总计：11 天**

### 10.2 每日任务拆解

**Day 1-2：基础搭建 + 库存模块**
- [ ] Spring Boot 项目初始化（依赖、配置、目录结构）
- [ ] MySQL 建表 + MyBatis-Plus 实体/Mapper
- [ ] Redis 配置 + Lua 扣减脚本编写
- [ ] 库存分桶逻辑：活动创建时计算分桶、写入 Redis
- [ ] 单元测试：验证 Lua 扣减的正确性（正常扣减/库存不足/重复购买）

**Day 3：异步下单 + 订单落库**
- [ ] 本地队列实现（LinkedBlockingQueue）
- [ ] RocketMQ 生产者/消费者
- [ ] 订单消费者：幂等校验 + 落库
- [ ] 订单状态查询接口
- [ ] 端到端测试：扣库存 → 入队 → MQ → 落库 → 查询

**Day 4：Nginx 限流 + Sentinel**
- [ ] Nginx 配置：limit_req 限流 + 反向代理
- [ ] Nginx Lua 脚本：前置查 Redis 判断库存
- [ ] Sentinel 接入：限流规则 + 降级返回
- [ ] 后端前置校验：活动状态 + 用户去重 + 本地缓存库存

**Day 5：验证码 + 热 Key 治理**
- [ ] 验证码生成接口（图片验证码）
- [ ] 验证码校验（GETDEL 防暴力破解）
- [ ] Caffeine 本地缓存配置
- [ ] 优雅退出逻辑（@PreDestroy）
- [ ] 兜底补偿定时任务

**Day 6-7：前端页面**
- [ ] 秒杀列表页：商品卡片 + 倒计时 + 库存进度条
- [ ] 抢购流程：验证码弹窗 → 排队等待 → 轮询结果
- [ ] API 封装（fetch + 错误处理）
- [ ] CSS 样式美化
- [ ] 与后端联调

**Day 8-9：管理后台 + 部署**
- [ ] 管理后台页面：活动管理 + 库存调整 + 监控看板
- [ ] 管理后台 API
- [ ] Docker Compose 编写
- [ ] 一键部署测试
- [ ] README 文档

**Day 10-11：压测 + 文档**
- [ ] JMeter 压测脚本编写
- [ ] 单机压测：记录 QPS、RT、成功率
- [ ] 对比测试：分桶 vs 不分桶、有缓存 vs 无缓存
- [ ] 压测报告撰写
- [ ] 架构设计文档撰写
- [ ] 简历内容更新

---

## 十一、简历写法参考

### 项目描述

> 面向电商高并发场景的秒杀平台，覆盖从用户点击到订单完成的全链路，重点解决库存热点、流量洪峰、缓存一致性三个核心问题。

### 核心亮点（4 条）

**1. 库存分桶 + Lua 原子扣减**

> 设计**库存分桶方案**：将单 Key 拆为 N 个子 Key，用户 ID 取模路由到不同分桶，配合 Lua 脚本保证查询与扣减的原子性；Redis Cluster 下不同分桶分散到不同槽位，写并发能力提升 N 倍。结合 Caffeine 本地缓存 + 热 Key 打散治理热点问题。

**2. 四层流量过滤漏斗**

> 构建**四层流量过滤体系**：前端图片验证码消峰防刷（GETDEL 防暴力破解）→ Nginx IP 限流 + Lua 前置校验售罄直接返回 → 后端 Sentinel 熔断降级 + 本地缓存前置判断 → 异步下单削峰填谷，逐层过滤无效请求，最终到达数据库的流量不足原始流量的 5%。

**3. 异步下单 + 优雅退出**

> 实现**本地队列异步下单**：扣减库存成功后投递本地有界队列立即返回，消费端限速批量投递 RocketMQ 落库；监听 SIGINT 信号实现优雅退出（队列剩余数据转移到 MQ），兜底定时任务扫描超时中间状态数据保证最终一致性。

**4. 全栈可演示**

> 前端实现秒杀商品列表页 + 验证码抢购弹窗 + 订单状态轮询，后端提供管理后台（活动管理、库存调整、实时监控），Docker Compose 一键部署全部组件，支持浏览器端完整演示秒杀链路。

---

## 十二、参考资料

以下为本项目设计所参考的知识库条目：

- [[秒杀系统架构设计]] — 流量分层、异步下单、系统隔离
- [[高并发库存扣减方案]] — Redis+Lua、库存分桶、分布式 partition
- [[高并发发券系统设计]] — 异步发券、双层队列、优雅退出、资金安全
- [[高并发缓存策略与问题]] — 多级缓存、穿透/击穿/雪崩、热 Key 大 Key
- [[限流算法与实现]] — 五种限流算法、降级熔断、分层限流
- [[数据分区算法]] — 哈希槽、一致性哈希、分桶原理
